{
  "private": true,
  "scripts": {
    "build": "spago bundle --outfile dist/tunebank.js --bundle-type app --platform browser",
    "test": "spago test",
    "optimise": "npm run uglify --s dist/tunebank.js --output dist/tunebankmin.js --compress --mangle"
  },
  "devDependencies": {
    "purescript": "^0.15.15",
    "esbuild": "^0.25.9",
    "spago": "^1.0.3"
  },
  "dependencies": {
    "uglify": "^0.1.5",
    "uglify-js": "^3.5.11",
    "xhr2": "^0.2.1",
    "xmlhttprequest": "^1.8.0"
  }
}
